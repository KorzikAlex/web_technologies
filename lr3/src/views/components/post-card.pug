mixin postCard(post, currentUserId)
    .card.mb-3.shadow-sm
        .card-body
            .d-flex.align-items-start.mb-3
                img.rounded-circle.me-3(src=post.author.avatar || '/public/assets/default-avatar.png' alt=post.author.fullName style="width: 50px; height: 50px; object-fit: cover;")
                .flex-grow-1
                    h5.card-title.mb-0= post.author.fullName
                    p.card-text
                        small.text-muted= new Date(post.createdAt).toLocaleString('ru-RU')
            p.card-text= post.content
            if post.image
                img.img-fluid.rounded.mb-3(src=post.image alt="Изображение к посту")
            if post.authorId == currentUserId
                .d-flex.justify-content-end.gap-2
                    button.btn.btn-sm.btn-outline-primary.edit-post(data-id=post.id)
                        i.bi.bi-pencil.me-1
                        | Редактировать
                    button.btn.btn-sm.btn-outline-danger.delete-post(data-id=post.id)
                        i.bi.bi-trash.me-1
                        | Удалить

