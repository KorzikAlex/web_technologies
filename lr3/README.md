# Social Network - Лабораторная работа №3

## 📋 Описание проекта

Веб-приложение социальной сети с использованием Node.js, Express, TypeScript, Pug, SCSS и Bootstrap.

## 🏗️ Архитектура

### Две сборки клиентской части:

#### 1. **Webpack-сборка** (используется сервером)
- **Папка:** `public/webpack-build/`
- **Назначение:** Основная рабочая сборка для production
- **Что делает:**
  - Компилирует TypeScript клиентских скриптов
  - Компилирует SCSS в CSS и встраивает в JS-бандлы
  - Минифицирует код
  - Создает бандлы с Bootstrap стилями
  - Генерирует HTML файлы из Pug (для справки)

#### 2. **Gulp-сборка** (альтернативная демонстрация)
- **Папка:** `public/gulp-build/`
- **Назначение:** Демонстрация альтернативного подхода к сборке
- **Что делает:**
  - Компилирует TypeScript клиентских скриптов в отдельные JS файлы
  - Компилирует SCSS в отдельный CSS файл
  - Копирует статические ресурсы (шрифты, иконки)
  - Генерирует статические HTML файлы

### Серверная часть:
- **Папка:** `dist/`
- Компилированный TypeScript сервера
- Рендерит Pug-шаблоны в HTML
- Подключает Webpack-бандлы для клиентской интерактивности

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
npm install
```

### 2. Режим разработки (Development)
Запускает одновременно:
- Сервер с hot-reload (nodemon)
- Webpack в watch-режиме
- Gulp в watch-режиме

```bash
npm run dev
```

Сервер будет доступен по адресу: **https://localhost:3000**

### 3. Production сборка и запуск

#### Шаг 1: Сборка
```bash
npm run build
```

Эта команда выполняет:
1. `npm run build:server` - компиляция серверного TypeScript → `dist/`
2. `npm run copy:assets` - копирование SSL, views, data в `dist/`
3. `npm run build:webpack` - сборка клиентской части через Webpack
4. `npm run build:gulp` - сборка клиентской части через Gulp

#### Шаг 2: Запуск сервера
```bash
npm start
```

Сервер будет доступен по адресу: **https://localhost:3000**

## 📁 Структура проекта

```
├── src/
│   ├── server.ts              # Главный файл сервера
│   ├── client/                # Клиентские TypeScript скрипты
│   │   ├── users-page.ts
│   │   ├── friends-page.ts
│   │   └── feed-page.ts
│   ├── routes/                # Express роуты
│   │   ├── users.ts
│   │   ├── friends.ts
│   │   └── posts.ts
│   ├── domain/                # Бизнес-логика
│   │   ├── UserManager.ts
│   │   └── PostsManager.ts
│   ├── models/                # Модели данных
│   │   ├── User.ts
│   │   └── Post.ts
│   ├── views/                 # Pug шаблоны
│   │   ├── layout.pug
│   │   ├── users.pug
│   │   ├── friends.pug
│   │   └── feed.pug
│   ├── public/
│   │   ├── scss/              # SCSS стили
│   │   └── assets/            # Статические ресурсы
│   ├── data/                  # JSON данные
│   │   ├── users.json
│   │   └── posts.json
│   └── ssl/                   # SSL сертификаты
│
├── public/                    # Скомпилированные клиентские файлы
│   ├── webpack-build/         # Webpack сборка (используется сервером)
│   └── gulp-build/            # Gulp сборка (для демонстрации)
│
├── dist/                      # Скомпилированный серверный код
│
├── webpack.config.ts          # Конфигурация Webpack
├── gulpfile.ts                # Конфигурация Gulp
├── tsconfig.json              # TypeScript конфиг (сервер)
├── tsconfig.client.json       # TypeScript конфиг (клиент)
└── package.json               # Зависимости и скрипты
```

## 🔄 Как работает взаимодействие клиента и сервера

### При запуске сервера (`npm start`):

1. **Сервер получает запрос** (например, `/users`)
2. **Express роут** обрабатывает запрос
3. **Pug-шаблон рендерится** на сервере в HTML
4. **В HTML встраивается ссылка** на Webpack-бандл:
   ```html
   <script src="/webpack-build/users.bundle.js" defer></script>
   ```
5. **Браузер загружает HTML** и **скачивает JS-бандл**
6. **JS-бандл содержит:**
   - Клиентскую логику (TypeScript)
   - Стили Bootstrap (встроенные через style-loader)
   - Интерактивность страницы

### Почему нужны обе сборки?

По условиям задания требуется продемонстрировать **два инструмента сборки**:

- **Webpack** - современный бандлер (основная рабочая сборка)
- **Gulp** - таск-раннер (альтернативная сборка для демонстрации)

**Сервер использует только Webpack-сборку**, но задание требует наличия обеих.

### Если удалить webpack-build:
❌ Сервер не сможет подключить клиентские скрипты и стили
❌ Страницы потеряют Bootstrap стили и интерактивность
❌ Приложение не будет работать

### Если удалить gulp-build:
✅ Сервер продолжит работать нормально
✅ Но не будет выполнено условие задания о двух сборках

## 📜 Доступные NPM скрипты

| Команда | Описание |
|---------|----------|
| `npm start` | Запуск production сервера |
| `npm run dev` | Запуск в режиме разработки (все в watch-режиме) |
| `npm run build` | Полная production сборка |
| `npm run build:server` | Компиляция только серверного кода |
| `npm run build:webpack` | Сборка только через Webpack |
| `npm run build:gulp` | Сборка только через Gulp |
| `npm run copy:assets` | Копирование статических файлов в dist |
| `npm run dev:server` | Только dev-сервер с nodemon |
| `npm run dev:webpack` | Только Webpack в watch-режиме |
| `npm run dev:gulp` | Только Gulp в watch-режиме |

## 🔐 SSL сертификат

Проект использует HTTPS. SSL сертификаты находятся в `src/ssl/`.
При первом открытии браузер может показать предупреждение о самоподписанном сертификате - это нормально для разработки.

## 🌐 Доступные страницы

- **https://localhost:3000** - Главная (редирект на /users)
- **https://localhost:3000/users** - Управление пользователями (Webpack)
- **https://localhost:3000/users/:id/friends** - Друзья пользователя (Webpack)
- **https://localhost:3000/posts/feed/:userId** - Лента новостей (Webpack)

## 🛠️ Технологии

- **Backend:** Node.js, Express, TypeScript
- **Frontend:** TypeScript, Bootstrap 5, SCSS
- **Шаблонизатор:** Pug
- **Сборка:** Webpack 5, Gulp 5
- **Инструменты разработки:** Nodemon, TSX, ESLint, Babel

